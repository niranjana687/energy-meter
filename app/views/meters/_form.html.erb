<%= form_with(model: meter, local: true) do |form| %>
  <% if meter.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(meter.errors.count, "error") %> prohibited this meter from being saved:</h2>

      <ul>
        <% meter.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :energy_consumed %>
    <%= form.number_field :energy_consumed %>
  </div>

  <div class="field">
    <%= form.label :total_energy_credits %>
    <%= form.number_field :total_energy_credits %>
  </div>

  <div class="field">
    <%= form.label :energy_credits_rate %>
    <%= form.text_field :energy_credits_rate %>
  </div>

  <div class="field">
    <%= form.label :balance %>
    <%= form.number_field :balance %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
